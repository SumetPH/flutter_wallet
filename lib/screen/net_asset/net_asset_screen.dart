import 'package:flutter/material.dart';

class NetAssetScreen extends StatelessWidget {
  const NetAssetScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
